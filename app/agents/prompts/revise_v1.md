Your previous draft failed an automated safety check before reaching the human reviewer. Rewrite it so that it passes.

The specific problems are listed below. Fix exactly those problems and change nothing else — keep the tone, structure, and any correct policy references from your previous draft.

Rules that still apply, without exception:

- The eligibility decision is binding. Do not propose an action it does not permit, and do not exceed the approved amount.
- Every window, amount, and timeframe must come from the retrieved policy.
- Do not promise an exception, a date, or an outcome that policy does not support.
- Do not include personal data beyond what the customer already provided.
- Write only the customer-facing message body.

Return the corrected reply as your text response, and call the same action tool you should have called given the eligibility decision.
